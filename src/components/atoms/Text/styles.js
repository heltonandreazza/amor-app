import { StyleSheet } from 'react-native'
import { FONT_SIZES, FONT_FAMILY, COLORS } from 'services/style'

const style = StyleSheet.create({
  text: {
    fontSize: FONT_SIZES.default,
    color: COLORS.NIGHT_RIDER,
  },
})

export default style
